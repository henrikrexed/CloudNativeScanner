TopicScanner v2 has been deployed!

{{- if .Values.ingress.enabled }}
Access the UI at:
{{- range .Values.ingress.hosts }}
  {{- if $.Values.ingress.tls }}
  https://{{ .host }}
  {{- else }}
  http://{{ .host }}
  {{- end }}
{{- end }}
{{- else }}
To access the UI, run:
  kubectl port-forward svc/{{ .Release.Name }}-webui {{ .Values.webui.service.port }}:{{ .Values.webui.service.port }}
  Then open http://localhost:{{ .Values.webui.service.port }}
{{- end }}

Pipeline API:
  kubectl port-forward svc/{{ .Release.Name }}-pipeline-service {{ .Values.pipelineService.service.port }}:{{ .Values.pipelineService.service.port }}

LLM Provider: {{ .Values.llm.provider }} ({{ .Values.llm.model }})
pgvector: {{ .Values.pgvector.enabled }}
